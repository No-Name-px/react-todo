import React from 'react';
import './Button.css';

export default function Button() {
    return (
        <div className="wrapper">
            <button className="button button-reset">Add new task</button>
        </div>
    );
}
